export interface DashboardStats { totalStudents: number; totalTeachers: number; totalClasses: number; totalRevenue: number; todayAttendanceRate: number; smsSentToday: number; }
export interface MonthlyFee { month: string; total: number; }
export interface TopPerformer { studentName: string; roll: string; avgScore: number; grade: string; }
